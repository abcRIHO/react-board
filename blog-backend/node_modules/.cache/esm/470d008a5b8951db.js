let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_5b8‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_5b8‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_5b8‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_5b8‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_5b8‍.w("./api",[["default",["api"],function(v){api=v}]]);_5b8‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();







// import createFakeData from './createFakeData';

// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } = process.env; // ㅅㅂ 왜 빨간줄이

mongoose.connect(MONGO_URI, { useNewUrlParser: true }).then(() => {
    console.log('Connected to MongoDB');
////    createFakeData();
}).catch(e => {
    _5b8‍.g.console.error(e);
});

const app = new Koa();
const router = new Router();

// 라우터 설정 -> 첫 번째 파라미터에는 경로, 두 번째에는 해당 라우트에 적용할 미들웨어 함수
router.use('/api', api.routes()); // api 라우트 적용

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT가 지정되어 있지 않다면 4000을 사용
const port = PORT || 4000;
app.listen(port, () => {
    _5b8‍.g.console.log('Listening to port', port);
});